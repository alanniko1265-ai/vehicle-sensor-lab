# driver

内核模块实现目录。

已提供一个可继续填写的最小模块骨架和 Kbuild Makefile。先阅读：

1. `../docs/prerequisite_guide.md`；
2. `../docs/day55_start.md`；
3. 完成源文件中的 `TODO(Day55-1)` 和 `TODO(Day55-2)`。

骨架不会替你实现后续字符设备、`read`、`poll`、`ioctl` 或环形缓冲区。
